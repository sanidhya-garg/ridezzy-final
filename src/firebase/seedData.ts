// This is a one-time script to seed your Firebase database with initial job data
// Run this by importing and calling seedJobData() from your component or console

import { seedJobData } from './firebaseService';

// Uncomment the line below and run this once to populate your database
// seedJobData();

export { seedJobData };
